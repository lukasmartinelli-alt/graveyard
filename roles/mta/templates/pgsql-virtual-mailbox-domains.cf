user = {{ db_postfix_user }}
password = {{ db_postfix_pass }}
hosts = {% for host in groups['db'] %}{{ hostvars[host]['ansible_' + iface].ipv4.address }}{% endfor %}
dbname = {{ db_name }}
query = SELECT 1 FROM domains WHERE name='%s'
