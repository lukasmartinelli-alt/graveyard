---
- hosts: all
  sudo: yes
  vars:
    db_host: localhost
    db_name: mailgenic
    db_deploy_user: deploy
    db_deploy_pass: "passwörd"
    db_web_user: web
    db_web_pass: "test"
    db_dovecot_user: dovecot
    db_dovecot_pass: "127lkasfb"
    db_postfix_user: postfix
    db_postfix_pass: "12xfx_341"
    mail_dir: "/var/mail/%d/%n"
    mail_user: mail
    ssl_key: "/etc/ssl/private/smtp.mailgenic.com.pem"
    ssl_cert: "{{ ssl_key }}"
  pre_tasks:
    - name: copy server certificate and private key
      copy: src=smtp.mailgenic.com.pem dest={{ ssl_cert }}
  roles:
    - db
    - mta
    - mda
    - rainloop
    - logstash
    - devenv
