{
  "name": "worker",
  "version": "1.0.0",
  "description": "A worker takes a user defined program and an event and produces a result.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nats": "^0.4.4"
  }
}
